spec:
  name: signalx-db
  services:
  - name: signalx-db
    git:
      repo_clone_url: https://github.com/NVYCO/jsonbox-1
      branch: master
    build_command: npm install
    run_command: npm start
    envs:
    - key: MONGODB_URI
      value: "MONGODB_URI"
